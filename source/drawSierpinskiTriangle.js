export function drawSierpinskiTriangle(gl, n) {
	console.log(`Đang chạy thuật toán Tam giác Sierpinski với N = ${n}`);
	// TODO: Viết code vẽ Tam giác Sierpinski
	// Gợi ý: Vẽ tam giác lớn, đệ quy đục lỗ tam giác ngược ở giữa.
}
